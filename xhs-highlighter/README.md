# XHS Highlighter 插件

## 功能
1. 在小红书帖子页面选择文本后，可选择 `yellow / green / pink` 三种荧光色进行划线。
2. 支持点击“取消划线”移除当前选中高亮。
3. 在插件弹窗中集中查看已划线内容，并可点击帖子标题回访原帖。

## 本地加载
1. 打开 Chrome 扩展管理页 `chrome://extensions/`。
2. 开启「开发者模式」。
3. 点击「加载已解压的扩展程序」，选择 `xhs-highlighter` 文件夹。
